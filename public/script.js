function scrollToProjects () {
  window.scrollTo({
    top: 750,
    bottom: 0, 
    behavior: 'smooth'
  });

projects.addEventListener('click', function() {
  window.scrollTo({
    top: 750,
    bottom: 0, 
    behavior: 'smooth'
  });
});